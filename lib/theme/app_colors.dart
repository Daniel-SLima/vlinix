import 'package:flutter/material.dart';

class AppColors {
  // Cor Primária (Chumbo/Preto Suave) - Para AppBars e Botões Principais
  static const Color primary = Color(0xFF212121);

  // Cor de Destaque (Dourado V-Linix) - Para Ícones, Links e FABs
  static const Color accent = Color(0xFFC69C6D);

  // Fundo Geral (Branco Gelo) - Para manter a limpeza
  static const Color background = Color(0xFFFAFAFA);

  // Texto Secundário (Cinza)
  static const Color textSecondary = Color(0xFF757575);

  // Sucesso/Erro (Para mensagens)
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFD32F2F);
}
